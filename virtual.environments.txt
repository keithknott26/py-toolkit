#with pyhon 3.3 and above the venv module vomes builtin
#for prior versions 'virtualenv' can be used
#these allows to create isolated python environments
#Packages installed here will not affect the global Python installation. Virtualenv does not create every file needed to get a whole new python environment

#It uses links to global environment files instead in order to save disk space end speed up your virtualenv. 


#steps to verify virtualenv is installed
#virtualenv --version

#Creating a virtual env
#  virtualenv prjname

#activate the virtualenv
# source prjname/bin/activate

#to exit the virtualenv enter
#deactivate